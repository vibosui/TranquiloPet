{
  "name": "pet-marketplace",
  "version": "0.0.0",
  "private": true,
  "description": "Marketplace mobile que conecta tutores de pets a cuidadores.",
  "scripts": {
    "start": "npm run start --workspace=mobile",
    "android": "npm run android --workspace=mobile",
    "web": "npm run web --workspace=mobile",
    "lint": "npm run lint --workspace=mobile",
    "typecheck": "npm run typecheck --workspace=mobile"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22.11.0"
  }
}
