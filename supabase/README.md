# Supabase

Esta pasta armazenará a configuração gerada pela Supabase CLI, migrations, seed, Edge Functions e testes de autorização.

Não crie tabelas somente pelo painel remoto. O schema que sustenta o aplicativo deve ser reproduzível a partir dos arquivos versionados deste diretório.

