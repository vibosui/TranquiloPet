name: Apply incidents and event demo once

on:
  push:
    branches:
      - agent/hospeda-patas-foundation
    paths:
      - scripts/apply-incidents-event-demo.mjs

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/hospeda-patas-foundation
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply guarded source patch
        run: node scripts/apply-incidents-event-demo.mjs
      - name: Refresh package lock
        run: npm install --package-lock-only
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/mobile/src/app/'(app)'/hosting/'[eventId].tsx' package-lock.json
          git diff --cached --quiet && exit 0
          git commit -m "feat: add structured incidents and event demo wiring"
          git push origin HEAD:agent/hospeda-patas-foundation
